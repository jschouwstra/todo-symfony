{% extends 'base.html.twig' %}

{% block content %}
    <h1>Dummy</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dummy.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dummy.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dummy_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('dummy_edit', { 'id': dummy.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
